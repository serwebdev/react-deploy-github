const Home = () => {
  console.log('BASE_URL:', import.meta.env.BASE_URL);

  return (
    <div>
      <div className="container">
        <h2>Home</h2>
      </div>
    </div>
  );
};

export default Home;
